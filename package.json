{
  "name": "ahla-shabab",
  "version": "1.0.0",
  "private": true,
  "description": "جمعية خواطر أحلى شباب — Mobile app (Expo) + shared design system. The admin dashboard now lives in ahla-shabab-dashboard.",
  "workspaces": [
    "shared",
    "mobile"
  ],
  "scripts": {
    "mobile": "npm run start --workspace mobile",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "demo:build": "npm run demo:build:app",
    "demo:build:app": "cd mobile && npx expo export --platform web --output-dir dist",
    "demo": "node scripts/demo-origin.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
