# دليل العرض التوضيحي للعميل (Client Demo Walkthrough)
### تطبيق جمعية خواطر أحلى شباب — النسخة العرضية (Demo v1.4.0)

> **تحديث 2026-07-27:** بعد جولة مراجعة الجودة (QA) وإصلاح 17 ملاحظة. أهم ما تغيّر ويؤثر على طريقة العرض:
> البريد الإلكتروني أصبح **إلزامياً** في نموذج الاستشارة، وشاشة رمز التحقق تُوضّح صراحةً أنه **لا يُرسل بريد فعلي**،
> ورسائل الحفظ صارت تقول **"أثناء الجلسة الحالية فقط"** بدل "محفوظ على جهازك".
> يُرجى قراءة **القسم 12 (تحذيرات قبل العرض)** قبل أي عرض أمام العميل.

> **تحديث 2026-08-01 — جولة مراجعة جودة ثانية.** جرى التحقق من التغييرات كاملةً على متصفح
> (عرض 320 / 390 / 430 بكسل): **22 من 24 متطلباً مكتملة، بلا أي عطل حرج أو عالي**.
> ما تغيّر منذ آخر تحديث ويؤثر على طريقة العرض:
>
> 1. **لوحة التحكم انتقلت إلى مستودع مستقل** (`ahla-shabab-dashboard`) ولم تعد داخل هذا المشروع.
>    أمر التشغيل المشترك في **القسم 12.3** تغيّر تبعاً لذلك — اقرأه قبل أي عرض يشمل اللوحة.
> 2. **أُصلح تداخل زر "تبرع" البارز** مع زر "اكفل أسرة" الثابت أسفل شاشات الأسر والتبرع واعرف عنا.
> 3. **تأكيد مهم للعرض:** لا شيء يبقى بعد إعادة تحميل الصفحة أو إغلاق التطبيق —
>    **لا تُعِد التحميل بين إرسال الاستشارة وتسجيل الدخول بنفس البريد**، وإلا ظهر السجل فارغاً.
>    التفاصيل في `qa/PERSISTENCE_REPORT.md`.

---

## 1. ملخص تحديثات تدفق التطبيق (Updated Application Flow)

تمت إعادة هيكلة تجربة المستخدم (UX) في تطبيق الهاتف المحمول لتتطابق بدقة مع متطلبات العرض التقديمي للعميل، مركزين على التصفح السلس والتبرع والخدمات الاستشارية:

```
[الشاشة الرئيسية (Home Screen)]
   ├─ قسم التبرع الأول (Donation Hero) ──► صفحة التبرع (Donate Tab)
   ├─ قسم الاستشارات الثاني (Consultations Hero) ──► مركز الاستشارات (Consultations Tab)
   ├─ معينات الحالات العاجلة ──► صفحة الحالات العاجلة (Urgent Cases Tab)
   └─ معاينة كفالة الأسر ──► كفالة أسرة (Sponsorship Screen)

[شريط التنقل السفلي - 5 عناصر (Bottom Navigation Bar)]
   ├─ 1. الأسر (Family Sponsorship / Cases)
   ├─ 2. الحالات العاجلة (Urgent Cases)
   ├─ 3. تبرع (Heart Button - Center Raised)
   ├─ 4. الاستشارات (Consultations Hub)
   └─ 5. اعرف عنا (About Us & Governorates)
```

---

## 2. فلسفة اختيار عناصر شريط التنقل السفلي الخمسة (Bottom Navigation Rationale)

تم اختيار الأزرار الخمسة بناءً على أولويات الجمعية والمستفيدين والمتبرعين:

1. **الأسر (`Cases` / "الأسر")**: يمثل الحالات الإنسانية وكفالة الأسر المتعففة، وهو العمود الفقري لعمل الجمعية.
2. **الحالات العاجلة (`UrgentCases` / "الحالات العاجلة")**: يتيح للمتبرع الوصول السريع للحالات الكارثية أو الطارئة التي تتطلب تدخلاً فورياً.
3. **زر التبرع الأوسط (`Donate` / "تبرع")**: تم تصميمه بارزاً في منتصف الشريط مع أيقونة القلب المركزية لتسهيل إجراء التبرع من أي مكان في التطبيق بنقرة واحدة.
4. **الاستشارات (`Consultations` / "الاستشارات")**: يعكس المحور التنموي والخدمي المجاني للجمعية (استشارات نفسية، أسرية، تربوية، قانونية).
5. **اعرف عنا (`About` / "اعرف عنا")**: يعرّف بالجمعية، رسالتها، ونطاق عملها الميداني في المحافظات.

---

## 3. سبب اختفاء شريط التنقل السفلي في الشاشات الداخلية (Internal Screens Behavior)

- عند فتح أي شاشة تفصيلية (مثل: تفاصيل الحالة، نموذج طلب الاستشارة، تفاصيل المحافظة، خطوات التبرع، تفاصيل المقال)، **يختفي شريط التنقل السفلي تلقائياً**.
- **الهدف من UX**:
  1. تركيز انتباه المستخدم تماماً على المهمة الحالية (إكمال التبرع أو تعبئة نموذج الاستشارة).
  2. منع التشتت أو الضغط الخاطئ على أزرار التنقل أثناء إدخال البيانات.
  3. إتاحة مساحة أكبر للشاشة وتفاصيل المحتوى.
  4. استخدام رأس شاشة نقي (Clean Header) مع زر العودة للخلف (Back Button) والمتوافق مع نظام أندرويد وآيفون.

---

## 4. تبسيط الصفحة الرئيسية: التبرع والاستشارات أولاً (Home Simplification)

- **حذف الأرقام والإحصائيات غير الموثقة من الرئيسية**: أُلغي قسم الإحصائيات الأرقامية (مثل 1.2M مستفيد) من الصفحة الرئيسية لضمان المصداقية.
  > **للدقة أمام العميل:** الأرقام **نُقلت** إلى صفحة "اعرف عنا" ولم تُحذف نهائياً. الصفحة الرئيسية خالية منها تماماً كما طُلب، لكن العميل سيراها في "اعرف عنا" — وهي **غير موثّقة** وتحتاج اعتماداً (انظر التوصية 4 في القسم 11). أصبحت الآن قابلة للتعديل أو الإفراغ من لوحة التحكم دون تعديل برمجي.
- **إزالة شريط المحافظات من الرئيسية**: تم نقل المحافظات إلى صفحة "اعرف عنا".
- **ترتيب الأهمية في الرئيسية**:
  1. **القسم الأول (تبرع الآن)**: بطاقة رئيسية كبرى تركز على العاطفة والواجب الإنساني لدعم الأسر.
  2. **القسم الثاني (احجز استشارة)**: بطاقة خدمية كبرى للتعريف بالاستشارات المجانية وحجز المواعيد.
  3. **معاينة الحالات العاجلة وكفالة الأسر ومشروع مميز**.

---

## 5. نقل المحافظات إلى "اعرف عنا" وتجربة تفاصيل المحافظة (Governorates UX)

- بدلاً من عرض المحافظات كشرائح زينة صامتة في الرئيسية، تم نقلها إلى صفحة **اعرف عنا** تحت قسم: **"نطاق عملنا في المحافظات"**.
- عند النقر على أي محافظة (مثل: القاهرة، الجيزة، أسيوط، قنا...):
  - تفتح شاشة نشاط المحافظة (`GovernorateActivityScreen`).
  - تعرّض للمستخدم:
    - نبذة عن نشاط الجمعية الميداني في هذه المحافظة.
    - الخدمات المتاحة (وصلات مياه، كفالة أسر، قوافل طبية).
    - الحالات المستحقة بالمحافظة مع زر تبرع مباشر.
    - المبادرات الجارية بالمنطقة.
    - مدى إتاحة الاستشارات لأبناء المحافظة مع زر حجز مباشر.

---

## 6. الفرق بين الزائر (Guest) والمستخدم المسجّل (Registered User)

- **الزائر (Guest)**:
  - يستطيع تصفح الشاشة الرئيسية، الحالات، الاستشارات، المحافظات، وتعبئة نموذج الاستشارة كاملاً في النسخة التجريبية.
  - لا يمكنه الوصول لبياناته الشخصية (سجل التبرعات، الإيصالات، الحجوزات السابقة، الإعدادات).
  - عند محاولة فتح أي قسم شخصي، تظهر بوابة ورود صديقة (`LoginGate`) توضح الفائدة:
    `"سجل دخولك لمتابعة حجوزاتك وتبرعاتك وإيصالاتك."`
    مع خيارين واضحين: **[تسجيل الدخول]** أو **[متابعة كزائر]**.

---

## 7. حفظ طلبات الاستشارة وتفادي تكرار الحسابات (Email Normalization & Guest Retention)

- **آلية الربط المحلي**:
  - عند تقديم الزائر لطلب استشارة، يتم توحيد البريد الإلكتروني عبر دالة `normalizeEmail(email)` (إزالة الفواصل وتحويل الحروف لصغيرة).
  - تقوم دالة `attachConsultationToDemoUser` بإنشاء أو العثور على سريل المستخدم المحلي بنفس البريد.
  - إذا قام نفس البريد بتقديم طلب استشارة آخر كزائر، **لا يتم تكرار الحساب**، بل يُضاف الطلب الجديد تحت نفس سجل المستخدم المحلي، ويتم تسجيل نشاط داخلي (`returning_guest_submitted_consultation`).
  - عند تسجيل الدخول مستقبلاً بنفس البريد (`loginDemoUserByEmail`)، يتم استرجاع كافة طلبات الاستشارة الحسابية المرتبطة بهذا البريد تلقائياً.
- **البريد الإلكتروني أصبح حقلاً إلزامياً (تحديث v1.4)**: كان اختيارياً، وهو ما كان يؤدي إلى دمج طلبات زوّار مختلفين تحت هوية واحدة. البريد هو **مفتاح الهوية** الذي تقوم عليه ميزة "الزائر العائد" بالكامل، ولذلك لم يعد من الممكن إرسال الطلب بدونه.
- **الرسالة الموجهة للمستخدم (بعد التصحيح)**:
  `"سُجِّل طلبك في النسخة التجريبية، ويمكنك تسجيل الدخول بنفس البريد أثناء هذه الجلسة لمتابعته."`
  وأسفلها تنبيه: `"نسخة عرض — الطلب محفوظ أثناء الجلسة الحالية فقط ولم يُرسل لأي جهة. سيُمسح عند إغلاق التطبيق."`
  > الصياغة السابقة كانت تقول "حُفظ الطلب على جهازك"، وهو ما لم يكن دقيقاً: البيانات في الذاكرة فقط وتُمسح عند إعادة التشغيل.

---

## 8. تدفق لوحة مقدم الاستشارة (Consultant / Service Provider Dashboard)

تم إنشاء وحدة مستقلة لعرض لوحة تحكم الخبير/المستشار (`ConsultantDashboardScreen`):

- **عناصر اللوحة الخمسة**:
  1. **نظرة عامة (Overview)**: عدد الحجوزات القادمة، مواعيد اليوم، الطلبات الجديدة، الجلسات المكتملة والملغاة.
  2. **مواعيدي والأنصبة (My Availability)**: مفتاح تفعيل/تعطيل استقبال الحجوزات، اختيار أيام العمل الأسبوعية، إضافة/حذف المواعيد المتاحة، وإضافة تواريخ الإجازات والاستثناءات، **وتعديل نطاق اليوم (من / إلى) — جديد في v1.4** (كان معروضاً للقراءة فقط).
  3. **الحجوزات والطلبات (My Bookings)**: فلترة وتصفية الحجوزات (جديد، مؤكد، مكتمل، ملغي) وبحث فوري باسم المتقدم أو البريد أو المحافظة.
  4. **نماذج المتقدمين والإجابات (Consultation Forms)**: عرض كافة الإجابات التفصيلية المرفوعة من المتقدم قبل الجلسة (العمر، المحافظة، وسيلة التواصل المفضلة، الإجابات المتخصصة، المرفقات)، مع أزرار الإجراءات السريعة (تأكيد، إكمال، إلغاء)، **وزر "إعادة جدولة" — جديد في v1.4** يفتح محرّراً مباشراً للتاريخ والوقت.
     > ملاحظة مقصودة: إعادة الجدولة **لا تغيّر حالة الحجز**. نقل موعد طلب "جديد" لا يعني تأكيده، ولذلك تبقى الحالة كما هي حتى يؤكّدها المستشار صراحةً.
  5. **الملف الشخصي (Profile)**: بيانات المستشار، التخصص، المؤهلات، أنواع الجلسات، ومعلومات التواصل.

---

## 9. ما هو تجريبي (Demo Only) وما يحتاج ربط مع الخلفية لاحقاً (Backend TODOs)

| الميزة | الحالة في العرض التوضيحي (Demo Now) | ما يلزم في الإنتاج الحقيقي (Future Backend) |
|---|---|---|
| رمز التحقق OTP | **لا يُرسل أي بريد إطلاقاً.** أي 6 أرقام تُقبل، والشاشة تقول ذلك صراحةً في تنبيه أصفر | ربط خدمة إرسال البريد (SendGrid/Resend) + API للتحقق |
| حفظ الاستشارات | في ذاكرة التطبيق فقط (`demoUsersStore`) — **يُمسح عند إعادة التشغيل** | `POST /consultations` وحفظ في قاعدة البيانات |
| لوحة المستشار | تحديث الحالات والمواعيد وإعادة الجدولة — **كلها في الذاكرة وتُمسح عند إعادة التشغيل** | `GET /me/provider/*` وحفظ دائم |
| التبرع | **لا تتم أي عملية دفع.** ينتهي المسار برقم مرجعي بحالة "قيد التأكيد" ولا يُعرض التبرع كناجح أبداً | ربط بوابة الدفع (فوري / إنستاباي / بطاقات بنكية) |
| أرقام الأثر في "اعرف عنا" | **قابلة للتعديل من لوحة التحكم** (إعدادات التطبيق ← أرقام الأثر) وتصل للتطبيق | تُقرأ من قاعدة البيانات |
| روابط التواصل الاجتماعي | مخفية حالياً لأنها غير مُعبّأة — تظهر تلقائياً عند إدخال روابط حقيقية | تُقرأ من الإعدادات |
| وصول الاستشارة إلى لوحة المستشار | **غير مربوط** — انظر التحذير رقم 2 في القسم 12 | ربط `consultation_requests` بحجوزات المستشار |

---

## 10. الترتيب المقترح للعرض أمام العميل (Presentation Order)

1. **افتتاحية التطبيق والتنقل السفلي**: استعراض الأزرار الخمسة (الأسر، الحالات العاجلة، تبرع، الاستشارات، اعرف عنا) وسلاسة التنقل.
2. **الصفحة الرئيسية البسيطة**: إبراز بطاقة التبرع أولاً ثم بطاقة الاستشارات ثانياً وعدم وجود أرقام غير موثوقة.
3. **تجربة المحافظات التفاعلية**: الانتقال إلى "اعرف عنا" ثم الضغط على إحدى المحافظات لاستعراض شاشة نشاط المحافظة.
4. **تسجيل الدخول بالبريد (Email OTP)**: تجربة إدخال البريد وتأكيد الرمز المكون من 6 أرقام.
5. **تقديم طلب استشارة كزائر**: تعبئة النموذج (**البريد إلزامي**)، استلام رسالة التأكيد مع الرقم المرجعي.
6. **إثبات ميزة "الزائر العائد"** — وهي أقوى ما في العرض: أرسل طلباً ثانياً بنفس البريد مع اختلاف حالة الأحرف (`Test@Example.COM` ثم `test@example.com`)، ثم سجّل الدخول بالبريد نفسه وأظهر أن **الطلبين معاً** ظهرا تحت حساب واحد دون تكرار.
7. **لوحة مقدم الاستشارة**: فتح "لوحة مقدم الاستشارة" واستعراض إدارة المواعيد، تعديل نطاق اليوم، الإجابات التفصيلية للمتقدمين، إعادة الجدولة، وتعديل حالات الحجز.
   > ⚠️ **لا تقدّم هذه الخطوة على أنها استكمال للخطوة 5.** الطلب الذي أرسلته للتو **لن يظهر هنا** — اللوحة تعرض بيانات تجريبية ثابتة. اعرضها كوحدة مستقلة. (التفاصيل في القسم 12)
8. **(اختياري) لوحة التحكم والتطبيق معاً**: شغّل `npm run demo` ثم عدّل "أرقام الأثر" في لوحة التحكم على `localhost:4000/admin/` وأظهر التغيير في شاشة "اعرف عنا" على `localhost:4000/`.

---

## 11. توصيات فريق التطوير (Engineering & Product Recommendations)

1. **الاعتماد النهائي لمسميات شريط التنقل**: التأكد من اعتماد العميل لمسمى "الأسر" بدلاً من "الحالات".
2. **تجهيز موفري خدمة البريد (Email OTP Service)**: التوصية باستقرار العميل على موفر خدمة البريد لإرسال رموز التحقق عند بدء تطوير الخلفية.
3. **تحديد معايير رفع مرفقات الاستشارات**: وضع حد أقصى لحجم ونوع الملفات المسموح بها في المرفقات.
4. **(v1.4) اعتماد أرقام الأثر — قرار مطلوب من العميل**: الأرقام المعروضة في "اعرف عنا" (`1.2M+ مستفيد`، `+650 مبادرة`، `+10,000 متطوع`، والمحطات الزمنية 2013–2025) **غير موثّقة**. أصبحت قابلة للتعديل من لوحة التحكم دون تعديل برمجي، لكن يلزم اعتماد الأرقام الحقيقية أو حذف القسم. أما ادعاء "22 محافظة" فقد أُزيل، ويعرض التطبيق الآن 12 محافظة فعلية مع عبارة "وفي توسع مستمر…".
5. **(v1.4) روابط التواصل الاجتماعي**: مطلوب تزويدنا بروابط الصفحات الحقيقية. حالياً كلها تشير لموقع الجمعية، ولذلك يُخفي التطبيق القسم بالكامل بدلاً من عرض أزرار تفتح الموقع نفسه.
6. **(v1.4) قرار الربط بين الاستشارات وحجوزات المستشار**: هل يتحوّل طلب الاستشارة المقبول إلى حجز لدى مستشار (مسار واحد)، أم يبقى صندوق وارد منفصل يعمل عليه الفريق يدوياً؟ القرار مطلوب قبل بناء الخلفية.
7. **(v1.4) الحفظ الدائم (Persistence)**: أهم فجوة تقنية متبقية. لا شيء يبقى بعد إغلاق التطبيق. يوصى بإضافة `AsyncStorage` أو — والأفضل — الانتقال مباشرة للخلفية الحقيقية، لأن ميزة "الزائر العائد" لا تُبرز قيمتها الحقيقية إلا إذا صمدت بعد إعادة التشغيل.


---

## 12. تحذيرات قبل العرض أمام العميل (Pre-Demo Warnings)

نتائج مراجعة الجودة (QA) — **يُقرأ هذا القسم قبل أي عرض**. الهدف تجنّب وعود لا يستطيع العرض الوفاء بها.

### 1) لا شيء يبقى بعد إغلاق التطبيق
تسجيل الدخول، طلبات الاستشارة، هوية الزائر العائد، مواعيد المستشار وحالات الحجوزات — **كلها في الذاكرة وتُمسح عند إعادة التشغيل**.

- **اعرض كل قصة في جلسة واحدة متصلة** دون إغلاق التطبيق أو تحديث الصفحة في منتصفها.
- التطبيق يقول ذلك بصراحة الآن ("محفوظة أثناء الجلسة الحالية فقط")، فلا داعي لإخفائه — لكن لا تَعِد بأن البيانات ستبقى.

### 2) الاستشارة المُرسَلة لا تصل إلى لوحة المستشار
هذان مخزنان منفصلان في النسخة التجريبية. الطلب الذي يُرسله الزائر يُحفظ في `demoUsers`، بينما لوحة المستشار تعرض ثلاثة حجوزات تجريبية ثابتة.

- **لا تعرض الخطوتين متتاليتين على أنهما مسار واحد.** إن سأل العميل: هذا هو الربط الأول المطلوب من الخلفية، وهو مُوثّق في `BACKEND.md §17`.

### 3) مزامنة لوحة التحكم مع التطبيق تحتاج أمراً محدداً
لأن المتصفح يفصل التخزين حسب المنفذ (origin)، لا تصل تعديلات لوحة التحكم إلى التطبيق إذا شُغّلا على منفذين مختلفين.

> **تغيّر في 2026-08-01:** لوحة التحكم لم تعد داخل هذا المستودع — انتقلت إلى مستودع
> مستقل: `ahla-shabab-dashboard`. الأمر القديم `npm run demo:build && npm run demo`
> لم يعد يشغّل اللوحة وحده.

استنسخ مستودع اللوحة **بجوار** هذا المشروع، ثم:

```bash
cd ../ahla-shabab-dashboard && DEMO_BASE=/admin/ npm run build
cd -  && npm run demo:build
ADMIN_DIR=../ahla-shabab-dashboard/dist npm run demo
```
يفتح الاثنين على منفذ واحد: التطبيق على `localhost:4000/` ولوحة التحكم على `localhost:4000/admin/`. حينها يظهر أي تعديل في التطبيق فوراً.

إن لم تُمرَّر `ADMIN_DIR`، يعمل التطبيق وحده على `localhost:4000/` ويطبع الخادم الأمر
اللازم لإضافة اللوحة — أي أن العرض لا يتوقف، تختفي فقط اللوحة.
للأجهزة الحقيقية: التصدير/الاستيراد JSON من **أدوات النظام**.

### 4) لا بريد ولا دفع — والتطبيق يقولها
- شاشة رمز التحقق تعرض تنبيهاً أصفر: *"نسخة عرض — لم يُرسل أي بريد إلكتروني. أدخل أي رمز مكوّن من 6 أرقام للمتابعة."*
- مسار التبرع ينتهي برقم مرجعي بحالة **"قيد التأكيد"**، ولا يُعرض أي تبرع كمكتمل. هذا سلوك مقصود ومطلوب.

### 5) صفحات المحافظات محتواها قالبي
كل محافظة تعرض نفس الخدمات الثلاث ونفس الحالتين ونفس المبادرة، مع تغيير الاسم فقط. ممتازة كعرض بصري — **لا تُقدَّم كبيانات حقيقية لكل محافظة**.

### 6) ملف APK
`ahla-shabab-v1.4.0-demo.apk` في جذر المشروع مبني من إصدار (versionCode 8) **ويحمل شعار الجمعية** كأيقونة. جُرّب على محاكي أندرويد وقت بنائه: يعمل، والتنقل وزر الرجوع سليمان، بلا أي انهيار.

> **تنبيه دقة (2026-08-01):** جولة المراجعة الثانية بَنَت نسخة إصدار جديدة بنجاح
> (`app-release.apk`) لكنها **لم تُثبَّت ولم تُشغَّل** — لا على محاكٍ ولا على جهاز.
> كل نتائج تلك الجولة من **المتصفح** فقط. الملاحظات أعلاه عن المحاكي تخص بناء
> `v1.4.0` السابق، ولا تنطبق تلقائياً على أي بناء أحدث.

> لم يُختبر على هاتف حقيقي. إن كان العميل سيمسك التطبيق بيده، يُفضَّل تجربة سريعة على جهاز فعلي قبل الجلسة — خاصةً لوحة المفاتيح وأشرطة النظام.
