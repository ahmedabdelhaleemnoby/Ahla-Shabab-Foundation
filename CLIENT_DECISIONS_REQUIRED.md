# قرارات العميل المطلوب حسمها (Client Decisions Required)
### جمعية خواطر أحلى شباب — اجتماع مراجعة المنتج

---

يرجى مراجعة النقاط والقرارات التالية مع إدارة الجمعية للحسم واكتفاء متطلبات مرحلة الإنتاج الحقيقي:

### 1. مسميات شريط التنقل السفلي (Bottom Navigation Labels)
- [ ] **الخيار أ (الموصى به)**: "الأسر" — للتعبير عن كفالة الأسر المتعففة وتفادي التكرار مع "الحالات العاجلة".
- [ ] **الخيار ب**: "الحالات" — المسمى العام المعتاد.

### 2. زر التبرع المركزي (Central Donation CTA)
- [ ] **الخيار أ (المعتمد في العرض)**: النص "تبرع" مع أيقونة القلب البارزة في منتصف الشريط.
- [ ] **الخيار ب**: النص "اتبرع".

### 3. القائمة الرسمية المعتمدة للمحافظات (Official Governorates List)
- [ ] الاعتماد النهائي لقائمة المحافظات الـ 12 الميدانية الحالية (القاهرة، الجيزة، الإسكندرية، الشرقية، المنوفية، المنيا، أسيوط، سوهاج، قنا، أسوان، الفيوم، شمال سيناء).
- [ ] تحديد الخدمات والمبادرات الخاصة بكل محافظة بشكل رسمي لمزامنتها مع قاعدة البيانات.

### 4. سياسة المفضلة والتصفح كزائر (Favorites & Guest Policy)
- [ ] هل تتطلب ميزة "المفضلة" تسجيل دخول رسمي لحفظ الحالات في حساب المستخدم، أم يُسمح بحفظها محلياً على الجهاز للزائر؟

### 5. التبرع السريع للزائر (Guest Donation Email Requirement)
- [ ] هل يلزم إدخال البريد الإلكتروني للزائر عند التبرع لإرسال إيصال التبرع الإلكتروني؟ أم يُكتفى برقم الهاتف أو التبرع المجهول؟

### 6. صلاحيات وطريقة وصول المستشارين للوحة التحكم (Consultant Portal Access)
- [ ] تحديد آلية تسجيل دخول مقدم الخدمة (هل يتم إنشاء حساب المستشار بواسطة مدير النظام فقط، أم عبر رابط مخصص برقم سري؟).
- [ ] تحديد ما إذا كانت الاستشارات تتم عبر زووم / تيمز، أم عبر مكالمات هاتفية وواتساب مباشرة.

### 7. دورة حياة حالة طلب الاستشارة (Consultation Status Workflow)
- [ ] اعتماد حالات الطلب الرسمية:
  1. `جديد` (عند التقديم).
  2. `قيد المراجعة` (من قبل فريق الفرز).
  3. `تم تحديد موعد` (بعد الاتفاق مع المستشار والمتقدم).
  4. `مكتمل` (بعد انتهاء الجلسة).
  5. `ملغي` (في حال تعذر التواصل أو الإلغاء).

### 8. إنشاء حساب تلقائي عند تقديم الاستشارة (Auto Account Creation)
- [ ] هل يؤدي تقديم الزائر لطلب استشارة إلى إنشاء حساب رسمي موثق على السيرفر تلقائياً بالبريد الإلكتروني، أم يظل الطلب مرتبطاً بالبريد فقط حتى يقوم بتأكيد بريده عبر OTP؟

### 9. موفر خدمة OTP للبريد الإلكتروني (Production Email Service Provider)
- [ ] اختيار موفر الخدمة المفضل لإرسال بريد رمز التحقق (مثل: SendGrid, Resend, Amazon SES, أو SMTP خاص بالجمعية).

### 10. المحتوى والصور الرسمية للمشروعات (Official Media & Copywriting)
- [ ] تزويد فريق التطوير بالنصوص الرسمية النهائية المعتمدة للجمعية والصور عالية الدقة للمشروعات والمبادرات الميدانية.

---

## قرارات جديدة ظهرت أثناء التنفيذ (Decisions raised during implementation)

آخر تحديث: 2026-08-08 — بعد مراجعة التسليم الكاملة. المرجع الكامل في `qa/`.

### 11. كود فودافون كاش «‎#237*9*‎» (Vodafone Cash donation code)
- [ ] **تأكيد الكود مع الجمعية.** تم تخزينه **حرفيًا كما ورد** في التعليمات، لكن هذا هو الشكل الذي
      يظهر به كود USSD عند نسخه من شاشة تعمل من اليمين لليسار. الصيغة القابلة للاتصال عادةً تكون
      `*9*237#`. **إن كان الكود المعروض خاطئًا فلن يصل تبرع المتبرع إطلاقًا.**

### 12. شهادة توقيع رسمية للتطبيق (Android release keystore)
- [ ] كل النسخ حتى v1.6.0 **موقّعة بشهادة الاختبار (Android Debug)** — مناسبة للتثبيت المباشر فقط.
      **Google Play سترفضها.** يلزم إنشاء شهادة إصدار رسمية وحفظها بشكل آمن قبل أي نشر على المتجر
      (فقدان هذه الشهادة يعني عدم القدرة على تحديث التطبيق لاحقًا).

### 13. خدمة إرسال البريد (SMTP) — يحجب اختبار تسجيل الدخول
- [ ] هذا هو **البند الوحيد المتبقي** الذي يحتاج طرفًا خارجيًا. بدونه لا يصل رمز التحقق، ولا يمكن
      إكمال تسجيل الدخول في الاختبار. مرتبط بالبند رقم 9 أعلاه.

### 14. ربط نشر التحديثات باختبارات CI (Gate deployments on CI)
- [ ] حاليًا يُنشر التحديث على الخادم عند كل دفعة إلى `main` **دون انتظار نتيجة الاختبارات**.
      إضافة `needs: [test]` سطر واحد، لكنها **ستمنع النشر عند فشل أي اختبار** — وهو قرار يخص سياسة
      الإصدار لدى الفريق وليس قرارًا تقنيًا.

### 15. أول تبرع حقيقي (First real donation — recommended test)
- [ ] قاعدة البيانات لا تحتوي على أي تبرع حتى الآن، أي أن مسار التبرع **لم يُختبر من طرف إلى طرف**
      مع مستخدم حقيقي. نسخة v1.6.0 هي أول نسخة قادرة على تسجيل تبرع فعلي على الخادم.
      يُنصح بتنفيذ تبرع صغير متعمَّد ومتابعة ظهوره في لوحة التحكم قبل الاعتماد عليه.

### 16. حساب خدمة Firebase لإشعارات الدفع (Firebase service account for push)
- [ ] كان البند رقم 49 في مصفوفة التسليم مسجّلًا كـ«محجوب في انتظار مفتاح FCM». المفتاح المطلوب
      (**FCM Server Key**) يخصّ واجهة FCM القديمة التي **أوقفتها Google نهائيًا في 20 يونيو 2024**،
      أي أنه لم يكن ليعمل حتى لو وصل.
- [ ] المطلوب فعليًا **حساب خدمة (Service Account)** من:
      Firebase Console ← Project settings ← Service accounts ← Generate new private key.
      يُوضع محتواه في متغيّر البيئة `FIREBASE_SERVICE_ACCOUNT` على الخادم. **لا يُرفع إلى المستودع.**
- [ ] ومطلوب أيضًا ملف **`google-services.json`** من **نفس مشروع Firebase**، لأنّ تطبيق أندرويد
      لا يستطيع الحصول على رمز الجهاز بدونه. يوضع في `mobile/google-services.json` ويُضاف السطر
      `"googleServicesFile": "./google-services.json"` داخل `android` في `mobile/app.json`.
      **لم يُضَف هذا السطر الآن عن قصد**: وجوده مع غياب الملف يُفشل بناء أي نسخة APK، وليس الإشعارات وحدها.
- [ ] بدون الاثنين معًا: الإشعارات تظهر داخل التطبيق كالمعتاد، ولا تصل إلى هاتف أحد.

### 17. أيقونة الإشعارات على أندرويد (Android notification icon — cosmetic)
- [ ] إعداد أيقونة/لون الإشعار يتطلّب `expo-notifications` كـ config plugin، وهو غير مُفعَّل حاليًا
      لأن الحزمة تُثبَّت في جذر المستودع بينما `expo` داخل `mobile/` — فيفشل تحميل الـ plugin عند البناء.
      النتيجة: الإشعار يعمل، لكن أيقونته هي أيقونة التطبيق الافتراضية (تظهر كمربّع أبيض على أندرويد).
      إصلاحه يخصّ ترتيب الحزم في المستودع لا الإشعارات نفسها.
